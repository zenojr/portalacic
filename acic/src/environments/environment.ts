// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {

    apiKey: "AIzaSyCOUGrrIuFjOkcawJxFXV5tCjT0SEtnflA",
    authDomain: "portalacic-cacador.firebaseapp.com",
    databaseURL: "https://portalacic-cacador.firebaseio.com",
    projectId: "portalacic-cacador",
    storageBucket: "portalacic-cacador.appspot.com",
    messagingSenderId: "779878158421",
    appId: "1:779878158421:web:88bb8e96c9d567675344dc"
  }
};


/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
